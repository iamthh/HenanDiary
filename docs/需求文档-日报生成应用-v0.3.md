# 桌面日报生成应用 — 需求文档 v0.3（终稿）

> 状态：**已确认**，可直接进入开发阶段。
> 最后更新：2026-09-14
> 变更说明：v0.3 为颠覆性简化——砍掉窗口追踪模块，改为纯截图驱动。
> **v0.4 变更（2026-09-15，M8）**：新增一站式 Web 主窗口需求。核心原则「安静」修订为
> 「启动即弹主窗口，关窗询问退出/托盘」；「托盘图标是它唯一的存在感」废止；
> F4.4 导出/清空已在 M8 落地；F6 开机自启仍不做。详见 docs/开发状态.md 的 M8 节。

---

## 一、产品定位

**一句话**：一个装完后不用管的 Windows 桌面小工具——它每隔几分钟给屏幕拍张照，每天晚上 22:00 让 AI 看这些照片写一份日报，你打开就能看。

**不是什么**：
- 不是员工监控软件（不做上报、不做后台管理、不给第三方看数据）
- 不是时间管理/to-do 工具（不强制你打标签、不打断你）
- 不是跨设备同步产品（首版单机）

**目标用户**：需要写日报但懒得回忆的打工人、需要记录工作产出的自由职业者、想复盘自己时间去向的人。

---

## 二、核心原则

| 原则 | 含义 |
|---|---|
| **安静** | 不弹窗、不打断、不要求打卡，托盘图标是它唯一的"存在感" |
| **隐私优先** | 数据只存本地；截图**绝不落盘**，AI 分析完即销毁 |
| **数据可见可控** | 用户能随时暂停采集、查看采集了什么、一键清空全部数据 |
| **开箱即用** | 下载→安装→填 Key→开始工作，全流程 ≤ 3 分钟 |
| **极简** | 一个采集动作（截图），一个产物（日报），不搞多余功能 |

---

## 三、关键决策（全部已确认）

| # | 决策点 | 结论 |
|---|---|---|
| D1 | 目标平台 | **仅 Windows 10/11** |
| D2 | 部署形态 | 本地 Python 常驻应用 + 系统托盘图标 |
| D3 | AI 额度 | **用户自备 API Key**，应用直连模型厂商，**无服务端** |
| D4 | 采集方式 | **纯截图**。每隔 N 分钟截一次全屏，送 AI 分析，**不做窗口追踪、不做空闲检测** |
| D5 | 截图处理 | **纯内存流处理，绝不写盘**（含临时目录）；AI 分析结果（文字）才作为素材保存 |
| D6 | 用户可见产物 | 只有**文字日报**，没有任何截图 |
| D7 | 日报生成时机 | 每晚 22:00 自动生成；次日 00:30 自动重新生成一次覆盖前一天 |
| D8 | 日报板块 | **三项：今日概览 / 主要工作块 / 时间分布** |
| D9 | 截图间隔 | **用户可选：10 分钟 / 5 分钟 / 2 分钟**，默认 5 分钟 |
| D10 | 数据量预估 | 8 小时 × 5 分钟间隔 = **96 张截图/天**，AI 输入量可控，无需复杂压缩 |
| D11 | 周报 | **做**。输入 = 汇总周一到周日的日报文本；**每周日 22:30 自动生成**；支持手动生成 |
| D12 | 数据路径 | **数据与安装路径完全分离**：安装路径默认 `C:\Program Files\`（用户可改），数据路径固定 `%APPDATA%\<应用名>\` |
| D13 | 原始数据保留 | **3 天**（截图分析文字，非图片） |
| D14 | AI 调用失败兜底 | **保留原始素材，下次成功调用 AI 时自动补生成**；托盘图标变红提示 |
| D15 | 日报查看方式 | **应用内窗口渲染 Markdown 显示** |
| D16 | 日报受众 | **纯自己看**，不做脱敏、不做"汇报体"美化 |
| D17 | 杀软误报 | **接受**。无代码签名，下载页明确提示"可能误报请加白名单" |
| D18 | 自动外发 | **首版不做**，预留接口 |

---

## 四、功能需求

### F1 采集模块（后台常驻）

**F1.1 定时截图**
- 间隔可选：10 / 5 / 2 分钟，默认 5 分钟
- **硬性约束**：截图以内存字节流直接送 AI，**禁止写盘任何位置（含临时目录、系统 temp）**
- 分析完成立即释放内存引用
- **明确告知用户**：因 Windows 虚拟内存机制，极端情况（内存不足）下内存页可能被换入 `pagefile.sys`，应用层无法杜绝
- 采集可全局一键暂停

**F1.2 采集控制**
- 托盘可一键「暂停采集 / 继续采集」
- 支持配置**工作时间段**（如 09:00-19:00 才采集）

### F2 日报生成模块

**F2.1 定时触发**
- 每天 22:00 自动执行（时间可配置）
- **次日 00:30 自动重新生成一次，覆盖前一天**（补 22:00 之后的内容）
- 机器休眠/关机场景：次日启动时补生成，日报标注"补生成"
- 支持手动「立即生成一份」

**F2.2 AI 输入构造**
- 输入：当日所有截图的 AI 分析结论（文字列表）
- 数据量：8 小时 × 5 分钟 = **96 条**，直接送模型，无需压缩

**F2.3 AI 输出（日报板块）**
- **今日概览**：一句话总结今天主要做了什么
- **主要工作块**：按项目/任务聚类的时间段 + 做了什么
- **时间分布**：各应用/类别占比（AI 根据截图内容推断）

**F2.4 模型接入**
- 用户填 API Key，OpenAI 兼容格式优先
- Key 存储用 Windows DPAPI 加密
- 支持配置 Base URL、模型名
- 调用失败需明确告知原因（Key 无效 / 余额不足 / 网络不通）
- **失败时保留素材，下次成功调用自动补生成**；托盘图标变红提示

### F3 周报模块

- 输入：周一到周日的 7 份日报文本
- 生成时机：**每周日 22:30 自动**
- 支持手动生成
- 输出：独立的周报文件（Markdown），与日报分开存储

### F4 查看与存储

**F4.1 数据目录结构**
```
%APPDATA%\<应用名>\
├── data.db              # SQLite 主库
├── reports/
│   ├── daily/           # 日报，永久保留
│   └── weekly/          # 周报，永久保留
├── config/
│   └── settings.json    # 用户配置
└── logs/                # 应用运行日志（轮转）
```

**F4.2 日报存储**
- Markdown 格式，按日期命名（如 `2026-09-14.md`）
- 同时保存一份结构化 JSON（给程序用，供周报汇总）

**F4.3 查看方式**
- 点亮托盘图标 → **应用内窗口渲染 Markdown** 显示当日日报
- 支持按日期列表翻历史日报/周报

**F4.4 数据管理**
- 「导出全部数据」：打包带走
- 「清空全部数据」：二次确认后彻底删除
- 截图分析结果**保留 3 天**，自动清理；日报/周报永久保留

### F5 首次使用引导

- 安装后首次启动，2~3 屏引导完成：
  1. 说明采集什么、存哪里（透明告知，含截图送 AI 的隐私说明）
  2. 请求/说明所需权限
  3. 填写 API Key 并**当场测试连通性**
- 引导结束前不开始采集

### F6 打包与分发

- 安装路径默认 `C:\Program Files\`，用户可修改
- 输出 Windows 安装包（exe/msi），普通用户双击可装
- 需处理：开机自启（可选）、托盘常驻、单实例运行
- 杀软误报：下载页明确提示，用户自行加白名单

---

## 五、非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 常驻内存占用目标 < 150MB；空闲时 CPU < 1% |
| 稳定性 | 连续运行 30 天不崩、不漏采；异常退出后能自动恢复并补采 |
| 隐私 | 除用户自己配置的 AI 接口外，不向任何第三方发送数据；不内置任何统计/埋点 |
| 可观测 | 本地日志（轮转），出问题能查；**禁止静默吞异常** |
| 兼容 | Windows 10 1809+ / Windows 11 |

---

## 六、明确不做（首版非目标）

- macOS / Linux 支持
- 账号体系、云端同步、多设备
- 团队/管理者视角的后台
- 日报自动外发（邮件/IM/网盘）
- 键盘内容记录、剪贴板内容记录
- **窗口追踪**（v0.3 已砍掉，纯截图驱动）
- **空闲检测**（截图本身就在记录）
- 浏览器 URL 采集
- 应用内复杂数据可视化图表
- 手机端查看
- 时区/系统时间被篡改的防护（技术细节，内部处理）

---

## 七、已知风险与诚实告知（将写入用户协议）

1. **截图送第三方模型**：截图内容会经过用户自己配置的模型厂商，需在引导第一屏明确告知并由用户主动确认。
2. **纯内存的局限**：应用层不主动写盘，但 Windows 虚拟内存机制下，极端情况（内存不足）可能将内存页换入 `pagefile.sys`，应用无法杜绝。
3. **杀软误报**：无代码签名，被杀软判定为可疑程序是预期内事件，下载页需提供加白名单指引。
4. **用户自备 Key 的门槛**：部分用户会因不知如何获取 API Key 而流失，需提供分步图文指引。
5. **截图是离散采样**：每 5 分钟一张，两张截图之间的事 AI 不知道。日报中的"时间分布"是 AI 推断，不是精确统计。

---

## 八、需求冻结

本文档经双方确认，**需求冻结**。后续变更需另行评审。

如需进入开发阶段，可直接基于本文档进行开发。
